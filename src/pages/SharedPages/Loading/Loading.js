import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center min-h-screen'>
            <button class="btn btn-square btn-primary loading"></button>
        </div>
    );
};

export default Loading;